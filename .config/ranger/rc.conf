map be shell nitrogen --save --set-zoom-fill %f
map bw shell wal -i %f
map bl shell wal -li %f

map bu shell git pull

map bX chain shell vim -o ~/.config/ranger/rc.conf %rangerdir/config/rc.conf; source ~/.config/ranger/rc.conf

map cw eval fm.execute_console("bulkrename") if fm.thisdir.marked_items else fm.open_console("rename ")

map f console scout -ftsea%space


set preview_images true
default_linemode devicons
